# GS-QRHub Environment Configuration

# Server
PORT=3000
SERVER_URL=https://qr.tools.domain.com
NODE_ENV=production

# Rate Limiting (requests per minute per IP)
RATE_LIMIT=100

# CORS - comma-separated list of allowed origins (* for all)
CORS_ORIGINS=*
